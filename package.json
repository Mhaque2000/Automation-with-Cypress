{
  "name": "diffwebel",
  "version": "1.0.0",
  "description": "Testing different types of web element",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "testHeaded": "npx cypress run --headed"
  },
  "cypress-cucumber-preprocessor": {
    "json": {
      "enabled": true,
      "output": "cypress/Reports/reports.json"
    }
  },
  "author": "Mahamood",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.1",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^13.6.2",
    "cypress-mochawesome-reporter": "^3.8.0"
  },
  "devDependencies": {
    "cypress-iframe": "^1.0.1",
    "cypress-xpath": "^2.0.1",
    "multiple-cucumber-html-reporter": "^3.6.1"
  }
}
